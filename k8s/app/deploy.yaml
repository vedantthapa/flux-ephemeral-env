apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  replicas: 1
  strategy: {}
  template:
    spec:
      containers:
      - image: nginx:1.24.0
        name: nginx
        resources: 
          requests:
            memory: "128Mi"
            cpu: "250m"
